{
  "rules": {
    ".read": false,
    ".write": false,
    "kanjiBattleRooms": {
      ".indexOn": [
        "expiresAt"
      ],
      "$roomId": {
        ".read": "auth != null && (auth.token.admin == true || auth.token.appAccess == true || (auth.token.email != null && auth.token.email.endsWith('@shibaurafzk.com'))) && (auth.token.admin == true || data.child('participants').child(auth.uid).exists())",
        ".write": false,
        "presence": {
          "$uid": {
            "$connectionId": {
              ".write": "auth != null && (auth.token.admin == true || auth.token.appAccess == true || (auth.token.email != null && auth.token.email.endsWith('@shibaurafzk.com'))) && auth.uid == $uid && root.child('kanjiBattleRooms').child($roomId).child('participants').child($uid).exists() && root.child('kanjiBattleRooms').child($roomId).child('participants').child($uid).child('abandoned').val() != true",
              ".validate": "newData.hasChildren(['connected', 'at']) && newData.child('connected').isBoolean() && newData.child('at').isNumber() && newData.child('at').val() <= now + 1000 && newData.child('at').val() >= now - 1000",
              "connected": {},
              "at": {},
              "$other": {
                ".validate": false
              }
            }
          }
        }
      }
    },
    "kanjiBattleRoomListings": {
      ".read": "auth != null && (auth.token.admin == true || auth.token.appAccess == true || (auth.token.email != null && auth.token.email.endsWith('@shibaurafzk.com')))",
      ".write": false,
      ".indexOn": [
        "listedAt",
        "expiresAt"
      ]
    }
  }
}
