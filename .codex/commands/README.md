# Codex Commands — math.app

このディレクトリは、既存の Claude 用コマンド運用を Codex からも同じ名前で参照できるようにするための入口です。

- 各ファイルは Codex 向けの参照ドキュメントです
- 実体の内容は原則として `.claude/commands/` 側と同期して扱います
- Codex はスラッシュコマンドを直接実行しないため、該当ファイルを読み、その手順・観点・期待出力に従って作業してください

共通ルールは必ず [GUIDELINES.md](file:///.agents/GUIDELINES.md) を優先してください。
